<h1>Deck#index</h1>
<p>Find me in app/views/deck/index.html.erb</p>

<%= link_to 'New deck', new_deck_path %>

<ul>
  <% @decks.each do |deck| %>
    <li>
      <%= link_to deck.name, deck_path(deck) %>
      <br>
      <%= deck.description %>
    </li>
  <% end %>
</ul>
